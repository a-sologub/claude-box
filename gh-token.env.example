# Copy to gh-token.env, chmod 600, never commit.
# Fine-grained token: github.com/settings/tokens?type=beta
# Scope it to only the repositories under CLAUDE_BOX_WORKSPACE that Claude
# should touch, Contents + Pull requests read/write. Set an expiry reminder.
GH_TOKEN=github_pat_REPLACE_ME
